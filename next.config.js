/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [], // falls du externe Bilder nutzt, hier Domains eintragen
  },
};

module.exports = nextConfig;
